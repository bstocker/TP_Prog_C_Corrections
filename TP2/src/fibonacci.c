#include <stdio.h>

void main() {
    int n = 7;
    int val[] = {0,1};
    printf("%d %d ", a, b);
    for(int i = 2; i < n; i++) {
        temp = val[0]+val[1];
        printf("%d ", temp);
        val[0]=val[1];
        val[1] = temp;     
    }
}
